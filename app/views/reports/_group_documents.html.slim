h1.pagination-centered Documents in Group
div style="min-height: 500px;"
	h3.fl.fb.lh35.no_margin Please select a group number from the drop down
	.mlm.form-search.fl
		ul class="nav nav-pills no_margin"
			
			li class="dropdown" id="menu1"
				a.dropdown-toggle data-toggle="dropdown" href="#menu1" id="menu1"
					|Select Group Number
					b class="caret mlm"

				ul class="dropdown-menu"
					- @group_numbers.each do |g|
						li
							= link_to 'Group Number "'+g+'"', reports_group_documents_path(:group_num => g), :remote => true, :title => "Group Number #{g}"

	#group_documents_results
		.clear
		h2.pagination-centered.pt100 Please select a group number from the dropdown